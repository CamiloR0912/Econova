<Page x:Class="Econova.Views.Pages.PaginaHistorial"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="White">

    <Page.Resources>
        <SolidColorBrush x:Key="AccentBrush"   Color="#2563EB"/>
        <SolidColorBrush x:Key="BorderColor"   Color="#D1D5DB"/>
        <SolidColorBrush x:Key="InputBg"       Color="#F9FAFB"/>

        <!-- Etiqueta de campo (igual que CrearReserva) -->
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="Margin"     Value="0,0,0,6"/>
        </Style>

        <!-- DatePicker personalizado (igual que CrearReserva) -->
        <Style x:Key="DateStyle" TargetType="DatePicker">
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="Foreground"      Value="#000000"/>
            <Setter Property="Height"          Value="38"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background"      Value="{StaticResource InputBg}"/>
            <Setter Property="Padding"         Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="38"/>
                                </Grid.ColumnDefinitions>

                                <DatePickerTextBox x:Name="PART_TextBox"
                                                   Grid.Column="0"
                                                   Foreground="#000000"
                                                   Background="Transparent"
                                                   BorderThickness="0"
                                                   FontFamily="Segoe UI"
                                                   FontSize="13"
                                                   VerticalAlignment="Center"
                                                   Padding="10,0,0,0"/>

                                <Button x:Name="PART_Button"
                                        Grid.Column="1"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Width="38" Height="38"
                                        Cursor="Hand"
                                        Focusable="False">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="btnBorder"
                                                    Background="Transparent"
                                                    CornerRadius="0,7,7,0">
                                                <TextBlock Text="&#xE787;"
                                                           FontFamily="Segoe MDL2 Assets"
                                                           FontSize="16"
                                                           Foreground="#2563EB"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="btnBorder"
                                                            Property="Background" Value="#EEF2FF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>

                                <Popup x:Name="PART_Popup"
                                       AllowsTransparency="True"
                                       Placement="Bottom"
                                       StaysOpen="False"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush"
                                        Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ColHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontFamily"        Value="Segoe UI"/>
            <Setter Property="FontSize"          Value="12"/>
            <Setter Property="FontWeight"        Value="SemiBold"/>
            <Setter Property="Foreground"        Value="#6B7280"/>
            <Setter Property="Padding"           Value="12,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="CellStyle" TargetType="TextBlock">
            <Setter Property="FontFamily"        Value="Segoe UI"/>
            <Setter Property="FontSize"          Value="13"/>
            <Setter Property="Foreground"        Value="#000000"/>
            <Setter Property="Padding"           Value="12,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming"      Value="CharacterEllipsis"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ══════ ENCABEZADO + FILTRO (estilo CrearReserva) ══════ -->
        <StackPanel Grid.Row="0" Margin="28,24,28,0">

            <!-- Header con icono (igual que CrearReserva) -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                <Border Width="40" Height="40" CornerRadius="10"
                        Background="#FEF3C7" Margin="0,0,14,0">
                    <TextBlock Text="&#xE81C;" FontFamily="Segoe MDL2 Assets"
                               FontSize="20" Foreground="#D97706"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Historial de Visitas"
                               FontFamily="Segoe UI" FontSize="18" FontWeight="Bold"
                               Foreground="#000000"/>
                    <TextBlock Text="Selecciona una fecha para consultar las visitas registradas."
                               FontFamily="Segoe UI" FontSize="12"
                               Foreground="#000000" Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>

            <!-- Tarjeta de selección de fecha -->
            <Border Background="White" BorderBrush="#E5E7EB"
                    BorderThickness="1" CornerRadius="12" Padding="24"
                    Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect Color="#000" BlurRadius="16"
                                      ShadowDepth="2" Opacity="0.06"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="280"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Fecha de consulta" Style="{StaticResource FieldLabel}"/>
                        <TextBlock Text="Elige el día que deseas consultar."
                                   FontFamily="Segoe UI" FontSize="12"
                                   Foreground="#6B7280"/>
                    </StackPanel>

                    <DatePicker Grid.Column="2"
                                SelectedDate="{Binding FechaSeleccionada}"
                                Style="{StaticResource DateStyle}"
                                VerticalAlignment="Center"/>

                    <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding ContadorTexto}"
                                   FontFamily="Segoe UI" FontSize="13"
                                   Foreground="#6B7280" VerticalAlignment="Center"
                                   Margin="0,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>

        <!-- ══════ TABLA ══════ -->
        <Border Grid.Row="1" Margin="28,0,28,28"
                Background="White" CornerRadius="12"
                BorderBrush="#E5E7EB"
                BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="#000" BlurRadius="16"
                                  ShadowDepth="2" Opacity="0.06"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="44"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Cabeceras -->
                <Border Grid.Row="0" Background="#F9FAFB" CornerRadius="12,12,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="#" Style="{StaticResource ColHeaderStyle}" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="CLIENTE" Style="{StaticResource ColHeaderStyle}"/>
                        <TextBlock Grid.Column="2" Text="SALA" Style="{StaticResource ColHeaderStyle}"/>
                        <TextBlock Grid.Column="3" Text="CÉDULA" Style="{StaticResource ColHeaderStyle}"/>
                        <TextBlock Grid.Column="4" Text="FECHA ENT." Style="{StaticResource ColHeaderStyle}"/>
                        <TextBlock Grid.Column="5" Text="HORA ENT." Style="{StaticResource ColHeaderStyle}"/>
                        <TextBlock Grid.Column="6" Text="FECHA SAL." Style="{StaticResource ColHeaderStyle}"/>
                        <TextBlock Grid.Column="7" Text="HORA SAL." Style="{StaticResource ColHeaderStyle}"/>
                    </Grid>
                </Border>

                <!-- Lista -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding VisitasFiltradas}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
                                    <Grid Height="48">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="50"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="100"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Numero}" Style="{StaticResource CellStyle}" HorizontalAlignment="Center" Foreground="#6B7280"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Cliente}" Style="{StaticResource CellStyle}" FontWeight="SemiBold"/>
                                        <TextBlock Grid.Column="2" Text="{Binding Sala}" Style="{StaticResource CellStyle}"/>
                                        <TextBlock Grid.Column="3" Text="{Binding Cedula}" Style="{StaticResource CellStyle}" Foreground="#6B7280"/>
                                        <TextBlock Grid.Column="4" Text="{Binding FechaEntrada}" Style="{StaticResource CellStyle}" FontSize="12"/>
                                        <TextBlock Grid.Column="5" Text="{Binding HoraEntrada}" Style="{StaticResource CellStyle}" FontSize="12"/>
                                        <TextBlock Grid.Column="6" Text="{Binding FechaSalida}" Style="{StaticResource CellStyle}" FontSize="12"/>
                                        <TextBlock Grid.Column="7" Text="{Binding HoraSalida}" Style="{StaticResource CellStyle}" FontSize="12"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Page>
